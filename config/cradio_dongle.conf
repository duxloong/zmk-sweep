# 开启 Dongle 模式（将当前设备作为中央主机，支持无矩阵组合）
CONFIG_ZMK_SPLIT_BLE_ROLE_CENTRAL=y

# 允许连接的最大 BLE 外设数量（左手和右手，共2个）
CONFIG_ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS=2

# 开启 ZMK Studio 支持（如果你想用官方的免刷机改键）
CONFIG_ZMK_STUDIO=y

# 增大蓝牙发射功率（可选，增强抗干扰）
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y